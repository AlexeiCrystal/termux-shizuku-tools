#!/data/data/com.termux/files/usr/bin/sh

. $PREFIX/shizuku/shizuku_functions.sh

if [ $(is_shizuku_installed) = "true" ]; then
   echo "Opening Shizuku..."
   open_shizuku
else
   echo "Shizuku not installed!"
fi
